name: Flutter APK Build

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. APKs require Java (the Android build toolchain needs it)
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17.x'

      # 2. Set up Flutter (includes the Dart SDK)
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      - name: Build Flutter app
        # Use the pipe "|" to run multiple commands in one step
        run: |
          # 1. Create the 'lib' folder Flutter expects
          mkdir -p lib
          
          # 2. Copy your code from 'bin' to the expected entry point
          cp bin/my_dart_action_demo.dart lib/main.dart
          
          # 3. Run the build sequence
          flutter pub get
          dart format .
          flutter build apk --release

      - name: Upload APK
        # This MUST be a separate step starting with "uses"
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          # Updated path for modern Flutter versions
          path: build/app/outputs/flutter-apk/app-release.apk